# 增量归档与录音接入

每次使用项目工作区中的 interview-workflow/state.md 和按群/会议保存的台账。没有历史游标不能声称已全量读取；先补核已授权话题范围。状态只存本地/获准团队位置，不写进可分享技能包。

## 增量处理

- 每群保存实际已读区间、最后成功处理消息的时间及可重定位原文、覆盖缺口。按群、消息时间、发送者、内容摘要值和附件引用去重。没有稳定消息ID时组合这些证据，不凭时间单独去重。
- 从已存锚点前一段重叠上下文开始读取，引用旧消息时定向回读；断点丢失时只扩展该群回溯。通知截断文本不能直接作为写表依据。
- 每候选/会议保存topic、供应商、来源、目标sheet/单元格/文档URL、上次agent写入值及保存验证。内部关联键不展示为个人专家ID。
- 正式表只填空白和追加。已有非空值不同则记待授权差异，不自动用新值替换旧值；已有链接单元格不追加字符串或替换链接。无合适空白位置时报告具体位置约束，不创建重复候选/重复topic来绕过保护。
- 处理成功或已归档成明确待办后才推进消息游标；失败和差异待办独立保留，下次继续处理。写入报错先检查是否已落地，再决定重试，避免重复。
- 多同事共享技能不等于共享进度。共同维护同一表需指定一个写入执行者，或使用共同受控台账和协作锁；未配置跨机器锁时不要声称可安全并发写入同一目标。

## 录音入口

用户在任务提供文件、路径或腾讯会议录制链接，并要求处理；或放入工作区 interview-workflow/inbox 后要求处理收件箱。文件出现本身不扩大上传目的地或话题授权。能由供应商、日期时间、职位与已存档期唯一关联时自动匹配；不能唯一匹配则只问缺失项。

优先使用已有带时间戳逐字稿。仅有录音时，在用户授权的腾讯会议上传转写路径操作，取回逐字稿并关联原录音；登录/上传受阻则记录，不把未转写标成完成。

记录音频内容指纹、路径、会议、转写链接、输出URL和状态：待匹配→待转写→待整理→待回听→已定稿。同文件重复提交续跑未完成任务；已有成稿只返回链接，除非明确要求修订或全新测试。

新纪要背景来自供应商，正文按会议顺序，疑点/数字荧光黄加时间点；实际检查排版保存后填入正式表匹配空白位置。用户修改同一在线文档，既有链接无需变更。人工定稿状态以用户确认或可验证操作为准，不能自动假定黄标已核实。
